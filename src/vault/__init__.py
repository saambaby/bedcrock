"""Wave C bridge stub for src.vault — real implementation lives in v0.1 branch.

This package exists only so test collection and integrated imports succeed.
All writers are no-ops; frontmatter helpers are minimal but functional so the
existing tests/test_vault.py module can be collected (it will likely fail at
runtime for the writer-dependent cases — acceptable per Wave C scope).
"""
